{% extends 'base2.html.twig' %}

{% block content %}

    {# <div class="container">
     {% if app.session.flashBag.has('success') %}
         <div class="alert alert-success">
             {% for msg in app.session.flashBag.get('success') %}
                 <p align="center">{{ msg }}</p>
             {% endfor %}
         </div>
     {% endif %}
 #}
    <div class="row mt">
        <div class="col-md-12">
            <div class="content-panel">


                <hr>
                <table class="table table-striped table-advance table-hover">
                    <h4><i class="fa fa-angle-right"></i> Migrant liste</h4>
                    <hr>
                    <thead>
                    <tr>
                        <th> Pseudo</th>
                        <th> Nom Complet</th>
                        <th> Age</th>
                        <th> Tel</th>
                        <th> Email</th>
                        <th> Langue</th>
                        {#      <th><i class=" fa fa-edit"></i> Status</th> #}
                        <th></th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for migrant in migrant %}
                        <tr>
                            <td> {{ migrant.nom }}</td>
                            <td> {{ migrant.prenom }}</td>
                            <td> {{ migrant.age }}</td>
                            <td> {{ migrant.tel }}</td>
                            <td> {{ migrant.email }}</td>
                            <td> {{ migrant.langue }}</td>
                            <td>
                                <button class="btn btn-success btn-xs"><i class="fa fa-check" ></i></button>
                                <button class="btn btn-danger btn-xs"><i class="fa fa-trash-o " onclick="window.location.href='{{ path('camp_supprimer_migrant',{'id':migrant.id}) }}'" ></i></button>

                            </td>
                        </tr>


                    {% endfor %}

                    </tbody>
                </table>
            </div>
            <!-- /content-panel -->
        </div>
        <!-- /col-md-12 -->
    </div>


{% endblock %}